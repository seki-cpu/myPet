# PawMatch 双问卷第二版增量设计书 v0.4

## 1. 本次目标

在现有 PawMatch 趣味测试基础上增加第二个问卷入口，使用户在完成语言选择后，可以选择：

1. **什么狗狗适合我？**：根据主人的性格、相处偏好和基本生活方式，从现有 12 个犬种中推荐更适合饲养的犬种。
2. **如果我是狗狗，我是哪种狗？**：保留现有趣味人格测试，题目、评分和结果逻辑均不修改。

本次只做增量实现。不得重写 CSS、共通组件、现有趣味问卷、分享功能或语言切换逻辑。

---

## 2. 页面流程

```text
现有语言选择页
  -> 新增问卷入口选择页
      -> 什么狗狗适合我？
          -> 复用现有答题页
          -> 复用现有结果页
      -> 如果我是狗狗，我是哪种狗？
          -> 进入现有趣味问卷
          -> 进入现有趣味结果页
```

建议继续沿用既定路由语义：

- `/{locale}/quiz/suitable`：适合饲养犬种问卷
- `/{locale}/quiz/personality`：现有趣味人格问卷
- 若仓库当前路由不同，保留现有路由形式，只增加等价入口，不为匹配本文而重构。

问卷入口选择页必须复用现有页面容器、标题、卡片、按钮和响应式样式。只增加文案和两张可点击入口卡，不新增 UI 库，不重新制作视觉系统。

入口文案建议：

| 入口 | 标题 | 简介 | 按钮 |
|---|---|---|---|
| 饲养适配 | 什么狗狗适合我？ | 从你的性格、生活节奏和相处方式出发，看看哪种狗狗更适合成为你的伙伴。 | 开始匹配 |
| 人格趣味 | 如果我是狗狗，我是哪种狗？ | 10 道趣味题，看看你的灵魂更像金毛、柴犬，还是自带剧情的哈士奇。 | 开始测试 |

---

## 3. 复用范围与禁止事项

### 3.1 必须复用

- 现有语言选择页及 `zh / ja / en` 切换机制。
- 现有答题页、`QuestionCard`、`OptionButton`、进度条、上一题按钮。
- 现有结果页、`ResultCard`、犬种图片、主题色、PNG、系统分享、下载和邮件逻辑。
- 现有 12 个 `breedId`、犬种名称和图片资源。
- 现有 sessionStorage 或状态管理方式。

### 3.2 禁止修改

- 不修改现有人格问卷的 `questionId`、`optionId`、评分向量、犬种原型和并列规则。
- 不修改现有 12 个 `breedId`。
- 不重写 CSS，不复制一套相同 CSS，不更换 UI 库。
- 不重写共通组件；如需区分内容，只增加 `quizType`、数据源或文案参数。
- 不让两个问卷共用同一份答案状态或同一份评分数据。
- 不把“人格相似”直接解释成“适合饲养”。

---

## 4. 现有犬种的二次利用

两个问卷共用同一组犬种 ID，但使用不同的画像和评分逻辑。

现有犬种：

- `golden-retriever` 金毛寻回犬
- `labrador` 拉布拉多
- `poodle` 贵宾犬
- `border-collie` 边境牧羊犬
- `shiba-inu` 柴犬
- `husky` 哈士奇
- `samoyed` 萨摩耶
- `german-shepherd` 德国牧羊犬
- `greyhound` 灵缇
- `corgi` 柯基
- `beagle` 比格犬
- `french-bulldog` 法国斗牛犬

新增独立数据文件或等价数据对象：

```ts
type QuizType = 'suitable' | 'personality';

type SuitableTraitKey =
  | 'activityCapacity'
  | 'companionshipNeed'
  | 'socialPreference'
  | 'trainingEngagement'
  | 'patience'
  | 'independenceFit'
  | 'groomingTolerance'
  | 'noiseTolerance'
  | 'aloneTimeFit';

type SuitableBreedProfile = {
  breedId: ExistingBreedId;
  traits: Record<SuitableTraitKey, number>;
  cautions: string[];
};
```

`suitableBreedProfiles` 只复用现有 `breedId`，不得覆盖现有人格测试的 `breedResults`。犬种画像用于表达常见倾向，不代表每一只个体，也不能代替领养机构、繁育者或兽医的具体评估。

---

## 5. “什么狗狗适合我”首版简要问卷

首版仍采用 **10 道单选题、每题 4 个选项、每次显示一题**，以最大限度复用现有页面。新题目使用独立 ID，例如 `S1`～`S10`；选项仍可使用稳定的 `A`～`D`，但不得与人格问卷答案混存。

### S1 你的理想周末通常是什么节奏？

- A. 大部分时间宅在家里，安静休息
- B. 慢慢散步、喝咖啡，偶尔出门
- C. 会安排运动、远足或较长时间的户外活动
- D. 越忙越开心，喜欢一整天都在外面探索

### S2 你每天能稳定留给狗狗多少运动和互动时间？

- A. 30 分钟以内
- B. 约 30～60 分钟
- C. 约 1～2 小时
- D. 2 小时以上，而且愿意安排训练或户外活动

### S3 你希望你和狗狗是什么相处模式？

- A. 各自安静待着，互不打扰也很舒服
- B. 平时独立，需要时互相陪伴
- C. 经常互动、散步、玩耍和拥抱
- D. 像形影不离的搭档，很多事情都一起做

### S4 面对陌生人和热闹场合，你通常是？

- A. 比较慢热，更喜欢熟悉的小圈子
- B. 安静观察，熟悉以后会放松
- C. 通常愿意认识新朋友
- D. 很享受热闹，人越多越有精神

### S5 如果狗狗反复学不会一件事，你更可能？

- A. 很容易失去耐心，希望它自然懂事
- B. 会再试几次，但不想每天训练
- C. 愿意调整方法，慢慢重复练习
- D. 会把训练当成长期兴趣，主动研究和记录

### S6 你更喜欢哪种性格的狗狗？

- A. 安静温和、情绪稳定
- B. 亲人黏人、很会表达喜欢
- C. 聪明敏锐、有自己的想法
- D. 活泼好奇、每天都可能制造新剧情

### S7 对掉毛、梳毛和定期洗护，你的真实接受度是？

- A. 希望越省事越好
- B. 可以接受少量日常清理
- C. 愿意每周多次梳理和打扫
- D. 能接受高频护理及定期专业美容

### S8 如果狗狗有点吵、兴奋或偶尔拆家，你会？

- A. 很难接受，我需要安静和秩序
- B. 偶尔可以，但希望大部分时间稳定
- C. 能接受，并愿意通过运动和训练改善
- D. 觉得这也是个性的一部分，我对混乱容忍度很高

### S9 普通工作日里，狗狗可能连续独处多久？

- A. 基本不会超过 2 小时
- B. 约 2～4 小时
- C. 约 4～6 小时
- D. 经常超过 6 小时

### S10 养狗时，你更像下面哪一种主人？

- A. 佛系陪伴型：不过度安排，希望相处轻松
- B. 温柔照顾型：重视情绪、安全感和稳定作息
- C. 规则引导型：愿意建立边界并坚持训练
- D. 搭档成长型：愿意投入大量时间，一起学习和挑战

---

## 6. 首版评分原则

1. 每个选项只写入 `SuitableTraitKey` 对应的分值，不直接给某个犬种加票。
2. 完成后生成主人的“适配向量”，再与 12 个 `suitableBreedProfiles` 计算距离。
3. 运动时间、独处时间、训练投入和护理接受度属于高权重维度；单纯“喜欢某种性格”不能覆盖基本饲养条件。
4. 输出距离最小的主推荐和第二匹配，沿用现有稳定排序方式，不使用随机数。
5. 若用户与某犬种在高权重维度明显不匹配，该犬种不得作为主推荐；可以在“需要考虑”中说明原因。
6. `french-bulldog` 等存在显著品种相关健康风险的犬种，结果页必须显示健康与费用提醒，不能只强调运动量低或适合公寓。

首版结果内容继续复用现有结果卡字段，只替换当前问卷对应的文案：

- 3 个关键词 → “适配关键词”
- 人格描述 → “为什么适合你”
- 可爱的小缺点 → “养之前要考虑”
- 第二匹配 → 保持现有第二匹配区域

推荐结果应使用“更可能适合”“值得进一步了解”等措辞，不使用“你一定要养”或“百分之百适合”。

---

## 7. 状态、多语言与数据隔离

- 三种语言继续使用相同的 `S1`～`S10`、`optionId`、适配向量和犬种画像。
- `zh / ja / en` 语言包新增入口页、10 题、40 个选项、结果说明及免责声明文案。
- 切换语言后必须保留当前问卷类型、题号和已选答案。
- 建议状态键区分问卷，例如：

```text
pawmatch:personality:v1
pawmatch:suitable:v1
```

- 结果 URL 或结果状态必须包含 `quizType`，防止同一 `breedId` 读取到另一套结果文案。
- 默认语言及 locale 回退规则完全沿用现有实现，不得借本次开发重构 i18n。

---

## 8. Copilot 最小实施顺序

1. 扫描现有真实路由、组件、状态键、稳定 ID 和语言包结构。
2. 在语言选择后的现有页面位置增加双问卷入口。
3. 将现有趣味问卷明确标记为 `personality`，但不改其题目和评分结果。
4. 新增 `suitable` 的题目数据、犬种适配画像和独立评分函数。
5. 通过 `quizType` 向现有答题页与结果页传入不同数据，不复制页面组件。
6. 补齐 `zh / ja / en` 对应键值。
7. 验证两个问卷互不覆盖答案，语言切换不丢失进度，现有 PNG/分享/邮件功能无回归。

如果仓库中的实际题数、犬种 ID、路由或组件名与本文不同，以现有代码为准；不得擅自重命名或删除，先报告差异再做最小映射。

---

## 9. 验收标准

- 语言选择后能看到两个清晰的问卷入口。
- 两个入口分别进入正确的问卷，返回后不会串题或串结果。
- 现有趣味问卷的 10 题、12 个结果和评分完全不变。
- 新问卷只从现有 12 个 `breedId` 中输出主推荐和第二匹配。
- 没有重写 CSS、共通组件、PNG、分享或邮件逻辑。
- 中文、日语、英语的入口、题目、结果和提醒均完整。
- `lint`、`typecheck`、现有测试和 `build` 全部通过。

